{% for type, messages in app.flashes %}
	<!-- correspond à un foreach php natif -->
	{% for message in messages %}

		<div class="alert alert-{{ type }}" role="alert">
			{{ message }}
		</div>

	{% endfor %}
{% endfor %}
